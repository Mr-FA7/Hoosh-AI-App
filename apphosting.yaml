runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 10

buildConfig:
  runtime: nodejs20

env:
  - variable: NODE_ENV
    value: production
    availability:
      - BUILD
      - RUNTIME
