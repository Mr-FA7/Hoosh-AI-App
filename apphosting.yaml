runConfig:
  concurrency: 80
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 10

buildConfig:
  runtime: nodejs20

scripts:
  buildCommand: npm run build
  runCommand: node server.js

env:
  - variable: NODE_ENV
    value: production
    availability:
      - RUNTIME
